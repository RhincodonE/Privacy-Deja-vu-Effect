name: fine-tune-env
channels:
  - pytorch
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - numpy
  - pandas
  - pytorch>=2.0    # will pull in the matching torch.nn, torch.optim, amp, etc.
  - torchvision
  - cudatoolkit=11.7  # adjust to your system’s CUDA version
  - tqdm
  - pip

  # pip-only packages
  - pip:
    - timm
